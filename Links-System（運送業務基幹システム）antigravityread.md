# Links-System（運送業務基幹システム） システム総合設計書 & 課題・不明点洗い出し仕様書

**作成日時**: 2026年8月8日  
**最終更新AI**: Antigravity (Gemini 3.6 Flash (High))  
**文書種別**: システム全般設計書 / 課題・不足点分析結果

---

## 1. システム目的・基本方針

**Links-System** は、運送事業における日々の運行・作業日報から、企業（取引先）・パートナー（協力会社・ドライバー）のマスタ管理、案件運賃単価、先払い（分割）計算、月次請求締め、支払締め発行処理までを一元管理する運送業務基幹システムです。

- **対象環境**: QNAP NAS（Container Station）および社内 LAN 環境
- **基本設計方針**: SPA（シングルページアプリケーション）＋ Node.js/Express RESTful API ＋ MariaDB によるオンプレミス/LAN内高速稼働

---

## 2. システムアーキテクチャ & 技術スタック

| 層 | 採用技術 | 説明・役割 |
|---|---|---|
| **フロントエンド** | HTML5 / Vanilla CSS / Vanilla JavaScript (SPA) | モジュール化された軽量SPA (`app.js`, `companies.js`, `partners.js`, `projects.js`, `daily_reports.js` 等)。ビルド不要で直接静的配信。 |
| **バックエンド API** | Node.js (v20+) / Express | RESTful API サーバー。認証 (`express-session`), 多重権限判定, 各種バリデーション, データCRUDおよび集計処理を提供。 |
| **データベース** | MariaDB 10.11 | RDBMS。セッション情報 (`express-mysql-session`) およびマスタ・トランザクションデータを一元保持。 |
| **環境構築** | Docker / Docker Compose | 開発用ローカル環境および本番 QNAP 環境に対応したコンテナ構成 (`docker-compose.yml`)。 |

---

## 3. ディレクトリ構造

```text
Links-System/
├── backend/                      # Node.js + Express API サーバー
│   ├── src/
│   │   ├── config.js            # 環境変数・接続設定
│   │   ├── db.js                # MariaDB コネクションプール管理
│   │   ├── migrate.js           # 自動SQLマイグレーション実行モジュール
│   │   ├── permissions.js       # 機能権限判定ロジック (RBAC)
│   │   ├── middleware/          # 認証・認可ミドルウェア (auth.js)
│   │   └── routes/              # 各業務APIルートハンドラー (14モジュール)
│   └── package.json
├── frontend/                     # SPA フロントエンド
│   ├── index.html               # アプリケーション基盤レイアウト・モーダル定義
│   ├── css/                     # 全体スタイル定義
│   └── js/                      # 画面別モジュール (14モジュール)
├── db/
│   └── migrations/              # DBマイグレーション定義 (001_〜012_)
├── docs/                        # AI作業記録 (`00_処理内容.md`)、モックアップ等
├── 仕様MD/                       # 業務仕様・画面仕様・各種ロジック設計書
├── docker-compose.yml           # コンテナオーケストレーション定義
├── Dockerfile                   # Express アプリ用 Dockerfile
└── README.md                    # 環境構築・起動・運用手順書
```

---

## 4. 主要機能および API モジュール設計

### 4.1 認証・ユーザー管理 (`/api/auth`, `/api/users`)
- **認証方式**: Express Session (Cookie ベース、`httpOnly`, `sameSite: lax`)
- **多重権限管理**: 1ユーザーに対して複数ロール（管理者, システム担当者, 経営者, 総務, 営業, パートナー, 企業）を割り当て可能。
- **権限検証**: `/api/permissions/check/:feature` による動的アクセス制御。

### 4.2 マスタ管理機能
- **企業マスタ** (`/api/companies`): 自社、取引先企業、支店・営業所拠点コード管理。
- **パートナーマスタ** (`/api/partners`): 協力会社、個人事業主ドライバー、支払口座、契約条件管理。
- **案件マスタ・価格セット** (`/api/projects`, `/api/price-sets`): 運行ルート・品目・案件単価・有効期間・改定履歴管理。
- **設定・参照マスタ** (`/api/master-settings`, `/api/lookups`, `/api/masters`): システム共通コード、カレンダー・休日区分管理。

### 4.3 業務・トランザクション機能
- **日報登録・一覧** (`/api/daily-reports`): 運行実績の登録、案件単価およびパートナー支払給与/手当の即時連動算出。
- **先払い（分割）管理** (`/api/advances`): 月3サイクル（第1〜第3）に基づく分割単価算出・前払い申請・承認管理。
- **請求締め・請求書発行** (`/api/invoices`): 取引先締日（10日, 15日, 20日, 25日, 末日等）に基づく売上集計・確定・請求書PDFデータ生成。
- **支払締め・支払明細発行** (`/api/payments`): パートナー締日に基づく受託費集計、先払い額・相殺費用の自動控除、支払明細生成。

---

## 5. データベース設計 & マイグレーション仕様

`db/migrations/` 配下に定義された 12 のマイグレーション SQL により、起動時にスキーマが自動初期化・更新されます。

```
001_users_and_sessions.sql          : ユーザー (`users`), セッション (`sessions`)
002_master_tables.sql              : 企業 (`companies`), パートナー (`partners`), 案件 (`projects`)
003_user_permissions.sql           : ユーザー機能権限マッピング (`user_permissions`)
004_login_roles.sql                : 権限ロール定義 (`roles`)
005_company_code_masters.sql        : 企業コード体系
006_transaction_and_partner_codes.sql: 日報 (`daily_reports`), 先払い (`advances`), 請求・支払トランザクション
007_feature_additions_draft.sql     : 拡張機能ドラフトテーブル
008_office_and_numbering.sql       : 拠点 (`offices`), 自動採番 (`numbering_rules`)
009_company_office_name.sql        : 拠点名称カラム拡張
010_price_set_lifecycle.sql        : 価格セット有効期間・ライフサイクル管理
011_price_set_architecture.sql     : 価格構造（売上単価・支払単価・手数料率）拡張
012_weekday_holiday_code.sql       : 平日・休日区分・単価増しコード定義
```

---

## 6. 仕様書と現状コードの乖離・不足点・不明点の詳細分析（設計補足）

本システムを運用・追加開発するにあたり、既存の『仕様MD』と現在の実装（Node.js/MariaDB）との間で発見された**乖離・不足点・不明点・設計上の検討事項**を以下に整理します。

### 6.1【不明点・仕様相違】データ保存先アーキテクチャの相違
- **現状の仕様書記述**: `仕様MD/個別機能仕様書：3-1. 先払い（分割）管理画面.md` 等の一部初期仕様書において「IndexedDB または LocalStorage を用いたローカル保存を前提」との記述が残存しています。
- **実際のシステム実装**: 現在は `backend/src/routes/advances.js` をはじめ、全機能が MariaDB + Node.js API による**サーバーサイド集中管理**として実装済みです。
- **設計決定・補足**: システムの整合性とセキュリティ（複数ユーザーによる同時閲覧・変更）のため、**MariaDB サーバーサイドDB管理を正（デファクト）**とし、仕様書の記述を更新・統一する必要があります。

### 6.2【不足点】案件単価（価格セット）の履歴・世代管理と過去日報への適用ロジック
- **検討課題**: 途中で案件単価が改定された場合、過去に登録済みの確定日報データの金額まで自動更新されてしまうリスクがあります。
- **設計補足**:
  - `010_price_set_lifecycle.sql` / `011_price_set_architecture.sql` で定義された `valid_from`（適用開始日）および `valid_to`（適用終了日）による世代管理を徹底する。
  - **原則ルール**: 日報登録時に「運行日」時点で有効な単価セットを取得・保持し、すでに「月次締め完了」となった日報データは価格セットが更新されても金額を再計算しない（ロックする）仕様を明記・追加する必要があります。

### 6.3【不足点】先払い（分割）金額算出と月次支払締めでの控除ロジック
- **検討課題**: 先払い管理で算出された「先払い可能額」および「振込手数料」が、月次支払締めでどのように控除・相殺されるかの計算式の厳密化。
- **設計補足**:
  - **差引支給額の計算式**:
    $$\text{最終支払額} = \text{月間受託合計額} + \text{各種手当} - \text{先払い振込済合計額} - \text{先払い振込手数料} - \text{その他控除額}$$
  - 先払いデータに `status`（`draft`: 下書き, `approved`: 承認済, `paid`: 振込済, `settled`: 支払締め完了）を追加し、`paid` の状態のデータのみを該当月の支払締め処理で自動参照・相殺ロックするフローを確定する必要があります。

### 6.4【不足点】消費税計算・端数処理・インボイス（適格請求書）対応仕様
- **検討課題**: 請求・支払における消費税端数処理（切捨て／四捨五入／切上げ）の単位（明細単位 vs 請求書合計単位）およびインボイス制度対応。
- **設計補足**:
  - **標準仕様**: インボイス制度に基づき、**「1請求書（1締め）あたり全明細の税抜金額を合算した後に、指定の消費税率（10%等）を乗じ、円未満切捨て」**をシステム標準とします。
  - 企業マスタ (`companies`) および パートナーマスタ (`partners`) に `tax_rounding`（切捨て / 四捨五入 / 切上げ）および `invoice_registration_number`（適格請求書発行事業者登録番号 T+13桁）のフィールドとバリデーションを明記・保持します。

### 6.5【不足点】締め確定・解除時の排他制御・二重締め防止
- **検討課題**: 請求締め・支払締めが実行された対象期間について、日報の追加・修正・削除を防ぐ排他制御（データロック）。
- **設計補足**:
  - `daily_reports` に `invoice_status`（`unprocessed` / `locked`）および `payment_status`（`unprocessed` / `locked`）を保持。
  - 締め処理実行時はデータベースの明示的トランザクション（`BEGIN ... COMMIT`）を使用し、締め完了フラグを付与する。
  - 締め解除操作（`unfinalize`）は「管理者」または「総務権限」を持つユーザーのみに限定し、操作ログ（監査ログ）を記録する設計を追記します。

---

## 7. アクセスコントロール・権限マトリクス (RBAC)

システム利用者のロールに応じたアクセス制限範囲を以下のように定義します：

| 機能モジュール | 管理者 | システム担当者 | 経営者 | 総務 | 営業 | パートナー | 企業 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **ユーザー管理** | ○ | ○ | × | × | × | × | × |
| **企業・パートナーマスタ** | ○ | ○ | 閲覧 | ○ | ○ | × | × |
| **案件マスタ・単価** | ○ | ○ | 閲覧 | ○ | ○ | × | × |
| **日報入力・閲覧** | ○ | ○ | 閲覧 | ○ | ○ | 制限 | 制限 |
| **先払い申請・承認** | ○ | ○ | 閲覧 | ○ | × | × | × |
| **請求締め・請求書発行** | ○ | ○ | 閲覧 | ○ | × | × | × |
| **支払締め・明細発行** | ○ | ○ | 閲覧 | ○ | × | × | × |
| **マスタ共通設定** | ○ | ○ | × | × | × | × | × |

---

## 8. 非機能要件・運用保守設計

1. **データバックアップ要件 (QNAP NAS)**
   - `scripts/nas-backup.sh` により、毎日深夜に `data/mysql` の自動バックアップを NAS 内バックアップ領域へ同期保存。
2. **パフォーマンス・接続設計**
   - 画面ロード時の不要なフル再読み込みを防ぐため、SPA 側のデータテーブル描画機能 (`data-table.js`, `ui_builder.js`) を活用し、差分取得・非同期レンダリングを実施。
3. **セキュリティ方針**
   - パスワードは `bcryptjs` (salt round 10) によりハッシュ化して保持。
   - API パスへのアクセス時はミドルウェア `requireAuth` および `requireRole` により不正アクセスの事前遮断を実施。

---

## 9. 今後のアクションプラン（仕様書の改訂要領）

1. **仕様MD の更新**:
   - 各個別機能仕様書内のレガシーな「IndexedDB / LocalStorage 保存」等の記述を「Express REST API 経由 MariaDB 保存」に記述修正。
2. **締め処理・ロック機構のテストコード拡充**:
   - 日報データの締め処理ロックおよび、二重締め・二重先払い申請を防止するバックエンドテストケースの構築。
3. **インボイス適格請求書出力フォーマットの精緻化**:
   - 登録番号表示、税率別（10% / 8%）合算計算、およびPDF/印刷用レイアウトの標準化。
