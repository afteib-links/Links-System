# 運送業務基幹システム：AI相談による決定事項・補完ログ

* **基盤マスターと計算ルールの管理（2026-09-12）**: 共通初期値はExcel同期ではなく、コード上の基盤マスター定義を正とする。新規構築・更新時に不足キーだけを追加し、既存値は上書きしない。論理削除・無効化は自動復元せず警告する。請求・支払計算は自由式へ全面移行せず、版付きハンドラーと安全式を組み合わせた型付きルールとし、公開前比較、適用版スナップショット、確定済み保護、既存下書きの選択再計算を必須とする。詳細は `計画/29_基盤マスターと計算ルール.md`。

* **料金登録UI・項目種別行・改定系列（2026-09-09、ユーザー画面確認済み、Issue #106）**: 金額データ詳細は料金カードと、料金項目名・項目種別・請求額・支払額・利益率・請求詳細名・支払詳細名の行で登録する。曜日色は月〜金=緑、土=青、日祝=薄赤、休=赤、ALL=黄で、ALLは排他。カードと同一種別行は上から先勝ち。初期表示は支払額まで、詳細名各列はカード幅の半分とし、詳細名は当面保存のみで帳票反映しない。項目種別は `fee_item_type`、保存は `fee_items_v2` と互換 `price_set_lines`。管理者だけが安全なExcel風の条件・請求・支払式を編集でき、未提供変数を含む行は下書き保存する。料金は企業単位の系列コードと改定版で管理し、過去修正理由・監査、未確定日報だけの再計算候補、コピー元系譜、基本改定の3点比較による選択反映を行う。確定・承認・締め済みスナップショットは変更しない。詳細は `計画/26_料金登録_項目種別行モデル設計.md`。

* **事務作業画面（2026-09-07、09-08補足）**: 日々の運用へ「事務作業」を追加し、企業→パートナー→日報・請求・支払→詳細プレビューの4列で対象月の進捗を横断確認する。既存の日報・請求・支払メニューは削除しない。`html_prototypes/14_office_work_codex.html` を画面構成、文字サイズ、ミラーカラム動作、詳細プレビュー配置の基本形とする。企業列上部の全対象では企業→パートナー→業務項目を第1～第3ミラーへ階層表示し、第3ミラーの日報・請求・支払ボタンで表示項目を絞り込む。企業・パートナーは選択のみ、業務項目はシングルクリックでプレビュー、ダブルクリックで既存入力画面を開く。1案件は直接、複数案件は案件選択を経由する。プレビューは主要情報をデスクトップ1画面に収めるが案件一覧は表示せず、日報は「未入力・入力済み・承認中・承認済み・請求支払」、請求・支払は「日報未承認・入力中・入力済み・承認中・承認済み・出力済み」で対象範囲を集計する。日報は「日報チェック」、請求・支払は既存帳票を開く「PDF表示」をヘッダーへ置く。案件一覧は遷移時に複数案件から選択する必要がある場合だけダイアログへ表示する。新しいDB・計算・帳票生成は追加せず既存APIを集約する。詳細は `個別機能仕様書：2-3. 事務作業画面.md`。

* **日報提出一覧（2026-09-06）**: パートナーから届く日報の受領を、先払と同じ5日系／10日系の月3回マトリクスで案件ごとに確認する。対象はパートナー未割当てを含む全個別案件。左列名は案件情報とし、主表示はパートナー名（作業員名）、補助が企業・案件名・番号・締日。各サイクルは対象期間・提出状態・提出日を横並びにして行を薄くする。対象月全体の総数は画面と表の最上段に出し、表フッターには置かない。3サイクルはすべて入力対象とし、「不要」セルは置かない。基本提出日は対象期間最終日＋1日（暦日）、期限は基本提出日＋全社共通の猶予日。遅延日数は提出済みなら提出日、未提出なら今日を判定日として自動計算し、セルで0〜365の整数へ手修正できる。提出トグルまたは提出日を変えたときは自動計算へ戻す。提出トグルONで提出日へ操作日を自動入力し、提出日の手修正を許可する。未来日は不可。日報入力データからの自動判定はしない。UIビルダー対象外。DBは `029_daily_report_submissions.sql`。詳細は `個別機能仕様書：2-2. 日報提出一覧画面.md` と `計画/24_日報提出一覧.md`。
* **収支分析3表の画面イメージ先行（2026-09-06）**: 入出金管理とは別に「収支分析」画面を置く。タブは収支分析一覧表、企業別利益率一覧表、稼働日一覧表。一覧は対象月1つ、エリア・担当者で抽出し、エリア／担当者／企業／契約区分（外注／給与・仮置き）で折りたたみ、エリア・担当者・企業の合計額表示を個別に切替する。企業行の請求金額は発行済み請求書合計を優先し、無ければ累計。利益率・稼働日は左が最新月の時系列。企業別利益率の赤色はマスター設定の利益率警告基準（%）未満とし、金額データと同じ設定値で変更する。エリアは担当者マスタの `area_name`。年度KPIグラフは対象外。詳細は `個別機能仕様書：4-1. 収支分析画面.md`。

* **マスターヘルプと一覧カラムの会社共通化（2026-09-06）**: マスター設定の各画面とハブにヘルプを置き、記載方法と影響画面をポップアップで案内する。マスター関連ポップアップは画面幅内に収め、内部は最大2列とする。UIビルダーの一覧表示列は管理者・システム担当者が保存し、全利用者へ共通適用する。対象はランチャー各機能の主一覧（請求・支払は2一覧、入出金は予定一覧）。先払マトリクス、カレンダー、日報月次グリッド、詳細内子テーブル、マスター小口一覧は対象外。保存先は `company_screen_layouts`。詳細は `計画/22_マスターヘルプとUIビルダー一覧項目.md`。

* **入出金・案件・日報・帳票画面改善（2026-09-06、Issue #83）**: 入出金は締日ラベル、土日祝の営業日シフト、口座期首残高と調整台帳（請求・支払・先払の自動反映なし）、カレンダー操作を入れる。基本案件・個別案件は属性とPriceSetをコピーする。日報操作列、見本PDFのA4紙面、支払控除ブロックを直す。DBは `027_source_bank_ledger.sql`（`026` は会社共通レイアウト）。詳細は `計画/21_入出金・案件・日報・帳票画面改善.md`。

* **Docker更新ツールの標準化（2026-09-06、Issue #75）**: ユーザーから「Docker更新」「Dockerを更新」等の依頼があった場合は、手動でコマンドを組み立てず、リポジトリ管理の `scripts/docker-update.*` を実行する。環境指定のない依頼は現在の作業環境だけを更新し、git同期やNASへの接続は行わない。NAS、ASUSTOR、QNAPが明示された場合だけNASモードを使用し、更新前バックアップ、`main` のfast-forward同期、Compose設定検証、再ビルド・起動、`/api/health` のDB接続確認までを一連で行う。ヘルスチェック失敗は非0終了とし、DBボリューム削除や `data/mysql` 削除は通常更新へ含めない。全AIから同じ手順を呼び出せるよう、プロジェクトSkill、常時Rule、`AGENTS.md` に起動条件を保持する。AIを使用しないWindows運用では、リポジトリ直下の `Docker更新.cmd` をダブルクリックし、同じPowerShell更新ツールとヘルスチェックを実行する。コンソールを自動で閉じず、成功・失敗と確認先URLを利用者が確認できるようにする。

* **先払管理画面の高密度レイアウト（2026-09-06）**: 添付画像は配置と情報密度だけの参考とし、画像内の名称・日付・金額は採用しない。既存の月選択、検索、絞り込み、集計、一括予定作成、CSV出力、固定見出し・左右列、編集・取消機能を維持する。各サイクルは、見出しを締め区分・支払予定日と支払額・手数料・先払／状態の小見出しに分け、案件行を入力・対象状態、元額・計算根拠、対象期間の順に高密度配置する。PCは1080pで8～10案件を見渡せる行高を目標とし、狭い画面は従来どおりマトリクス内だけを横スクロールさせる。API、DB、金額計算、保存上限、状態遷移は変更しない。

* **スマホメニュー・一覧の表示幅改善（2026-09-06、Issue #77）**: 760px以下で開いたサイドバーはシステム名、分類名、全機能名を省略せず表示し、1文字表示はPC・タブレットの折り畳み状態だけに限定する。一覧の数値識別子列は内容5文字相当へ狭め、企業・パートナー一覧の行操作はスマホ時に「操作」ボタンから開くモーダルへ集約する。先払一覧はスマホ時に案件別合計列の右固定を解除し、左案件列を約140pxへ縮めてサイクル金額の表示領域を確保する。PC表示、API、DB、業務計算は変更しない。

* **共通ヘッダーの1行コンパクト配置（2026-09-06、Issue #77）**: 添付写真の指示に合わせ、メニュー開閉、システム名、現在の画面タイトル、利用者、ログアウトをPC・タブレットでは共通ヘッダーの横1行へ集約する。画面タイトルは現行文字サイズを維持し、本文側の重複タイトルは削除する。760px以下はシステム名、520px以下は利用者名を省略し、メニュー、画面タイトル、ログアウトを優先して1行と固定高を維持する。詳細画面の「戻る」、サイドバー、stickyヘッダー、権限表示とログアウト機能は維持する。

* **共通UI・金額・日報・基本案件・先払画面の改善（2026-09-06、Issue #73）**: 全機能画面から「機能一覧へ」を削除し、本文側の画面タイトルと詳細画面の「戻る」、サイドバーのホーム導線は維持する。画面上の通常金額は整数円を全角円記号付きで表示し、単価は既存値に小数がある場合だけ末尾ゼロを省略して表示するが、入力・保存・計算は従来どおり整数円とする。金額データの料金項目は料金計算区分マスターから複数の計算種別を選択でき、日極・時間は曜日別、距離は全日として保存する。未知の計算種別は設定保存まで許可し、日報自動計算には使用しない。日報一覧カードは他条件を維持して状態絞込に使い、日報入力は操作部と列見出しを固定する。基本案件詳細には生成済み個別案件一覧を表示し、先払セルは期間、先払フラグ、状態、入力、元値を横方向へ再配置する。詳細は `計画/20_共通UI・金額・日報・基本案件・先払画面改善.md`。

* **請求・支払・金額データ・企業画面の見直し（2026-09-05）**: 請求・支払一覧は案件を常時表示し、月次承認後に作成可能とする。単独作成と同一条件の複数案件取纏を併用し、処理開始後も案件状態から同一帳票を開く。締日は案件設定を正とし、摘要だけの変更理由は不要、金額変更時のみ理由必須とする。下書き帳票は「見本・未発行」表示で確認可能とする。摘要テンプレートと表示順を金額データ・共通設定で管理する。企業画面は企業名を主、事業所を補助とし、弊社担当は履歴へ集約、請求書送付情報は請求先単位で保持する。詳細は `計画/19_請求支払・金額・企業画面改善.md`。

* **一覧・編集画面の操作性（2026-09-05、Issue #60）**: 主要一覧は列見出しのソート、見出し直下の列別絞り込み、シングルクリックの行選択、ダブルクリックの編集または閲覧へ統一する。副作用を伴う操作はダブルクリックで実行しない。日報以外の編集画面は見出し付きカードと項目内容に応じた入力幅へ整理し、企業・パートナー・案件・車両・担当者等の参照項目は手入力の部分一致検索に対応する。個別案件の車両は所有元を企業／パートナーから先に選び、案件に設定された企業またはパートナーの車両だけを候補とする。既存車両は所属が一意に判定できる場合だけ所有元を補完し、曖昧なものは再選択を求める。

* **スマホ縦画面の共通表示（2026-09-04、Issue #56）**: 760px以下では画面本体を常に幅100%の1列構成とし、サイドバーは初期状態で画面外へ閉じてハンバーガーボタンからオーバーレイ表示する。スマホのメニュー開閉はPCの折り畳み保存設定へ影響させず、背景操作、メニュー選択、Escキーで閉じる。月選択、検索、絞り込み、主要操作は画面幅内へ再配置し、請求・支払・先払・日報等の横長一覧はカード化せず、表領域だけを横スワイプ可能にする。ページ全体には横スクロールを発生させない。

* **全体UIとPDF書体の刷新（2026-09-04、Issue #56）**: 日本人の事務担当者がPCで利用する業務画面として、白・濃紺・青緑の共通デザイン、権限対応左サイドバー、最大1800pxの作業領域、14px以上の表文字、36～40pxの入力欄へ統一する。状態はグレーの未着手・取消、青の作業中、黄の確認待ち、緑の完了、赤の差戻し・エラーとして、アイコンと日本語ラベルを必ず併記する。ホームは権限内の月次状態と次操作を表示する。画面はMeiryo系ゴシック、PDFはDockerへ導入するBIZ UDPGothicを正とし、利用不可時の他言語フォントへの暗黙フォールバックを禁止する。既存の業務計算、帳票項目、確定済みPDFは変更しない。

* **PDFの単価・会社名・ロゴ表示（2026-09-04、Issue #56）**: 単価欄は負号付き7桁を通貨記号・桁区切り込みで、金額欄は8桁を通貨記号・桁区切り込みで改行せず表示できる必要最小限の幅とする。左余白をほぼなくして円記号を保護し、単価見出しは中央、数値は右揃えとする。単価・数量・金額は摘要より少し小さく字間を詰める。宛先・発行元会社名は1行表示し、長い名称は帳票内で縮小する。会社ロゴはPNG・JPEG・WebPを設定画面から選択または貼り付けでき、縦横比を維持して表示する。変更は新規発行・訂正発行分へ適用し、確定済みPDFは変更しない。

* **距離超過の月間結果の帰属（2026-09-02、Issue #42）**: 既存の日報計算は下書き中の最新自動計算を保持し、日次確認時・案件×月の承認時にスナップショットを固定するモデルである。このため、月間累計距離の結果は各日報行へ按分・重複保存せず、`daily_report_distance_monthly_results` の案件×対象月×請求／支払別の一意行へ帰属させる。日報行には月間方式の距離額を加算せず、月間結果APIと承認スナップショットから明細・金額を参照する。再計算は同じ一意行を版更新し、月次承認時はその時点の結果を確定スナップショットへ含める。確定後の料金設定変更は過去スナップショットを変更しない。

* **基本勤務時間の扱い**: 一律9時間ではなく、案件ごとに「拘束時間か実働時間か」を指定し、基準時間も案件に依存させる。
* **休憩時間の入力（2026-08-28）**: 休憩は1日の合計時間を`H:MM`形式（例: `1:00`）で1分単位で入力する。複数回の休憩がある場合は、入力者が合算した時間を1項目へ入力し、各休憩の開始・終了は個別登録しない。
* **深夜帯内休憩時間（2026-08-30）**: 日報へ1分単位の深夜帯内休憩時間を追加し、初期値を`0:00`とする。深夜対象時間からだけ控除し、合計休憩時間を超える入力は禁止する。深夜勤務と合計休憩があるのに`0:00`の場合は警告するが、明示的な続行により保存・日次確認を許可する。深夜計算区分が「対象外」の側では使用しない。請求・支払の深夜時間帯が同じ場合は共通入力とし、異なる場合は請求用・支払用へ自動分離する。条件が再び同じになった場合は値を維持して共通入力へ戻す。自動分離・統合は一般的な操作履歴と同じ期間保存する。
* **深夜時間の手動調整（2026-08-30）**: 自動計算した深夜時間は直接上書きせず、理由必須の深夜時間調整を正負の1分単位で別入力する。調整後時間は0分以上かつ当該側の計算対象勤務時間以下に制限する。請求・支払条件が同じ場合は共通入力、異なる場合は自動分離し、条件が同一へ戻れば値を維持して共通入力へ戻す。自動計算値、調整値、調整後時間、理由、入力者、日時と入力欄切替を履歴保存する。
* **超過・深夜・深夜超過の料金適用（2026-08-30）**: 日報画面の「超過」「深夜」「深夜超過」を独立した排他的な計算明細とする。時間は1分単位で、深夜帯かつ基準時間超過を深夜超過、基準時間超過のうち深夜超過を除いた時間を超過、深夜対象時間のうち深夜超過を除いた時間を深夜へ自動分類し、同じ1分を複数欄へ重複計上しない。各欄の最終時間へ対応する専用料金を適用し、請求額・支払額は料金設定で別々に登録する。深夜超過時間へ超過料金と深夜料金を自動で重複加算せず、必要な割増を含む深夜超過専用単価を適用する。深夜と深夜超過はそれぞれ独立して「別途計算」「基本料金に含む」「対象外」を請求・支払別に設定できる。自動候補と理由付き調整を反映した最終時間を各欄の計算数量として保存する。
* **日報行での料金単価の一時変更（2026-08-30）**: 料金単価エリアには、選択した料金区分の「基本加算」「超過」「深夜」「深夜超過」の契約単価を請求・支払別に初期表示する。一時的な変更がある場合は対象の日報作業行だけ単価を直接変更でき、料金マスタや他行へ反映しない。変更できるのは単価・固定額・割増率等の計算値だけとし、料金設定で定めた計算方式・計算式は日報上で変更しない。変更後の値を同じ計算式へ適用して再計算し、元の値、変更後の値、必須の変更理由、変更者、日時を保存して手動変更表示を付ける。日次確認後は変更不可とし、確認チェックを外してから修正・再確認する。
* **深夜・深夜超過の計算方式（2026-08-30）**: 深夜料金と深夜超過料金も通常料金と同じ料金設定・計算方式の仕組みを使用する。請求側・支払側ごとに、対象可否、計算方式、単価・固定額・割増率、計算対象時間を独立して設定でき、両側で異なる計算方式も許可する。日報上の一時変更では計算方式を変えず、設定済みの計算式へ変更後の値を適用する。
* **深夜・深夜超過の丸め条件（2026-08-31）**: 深夜・深夜超過だけに専用の丸め条件は設けず、案件に設定された通常の時間・金額の丸め条件を共通して適用する。通常超過、深夜、深夜超過ごとに異なる丸め単位・方式・段階は設定しない。請求側・支払側の差異と、明細ごと・日ごと・月合計後の金額丸めは、通常料金と同じ既定の仕組みに従う。
* **深夜・深夜超過の計算順序（2026-08-31）**: 「勤務時間と深夜時間帯の重複抽出→深夜帯内休憩の控除→理由付き深夜時間調整の反映→通常超過時間との重複判定→通常・超過・深夜・深夜超過への排他的分類→案件の時間丸め→料金計算後に設定段階で金額丸め」の順で計算する。時間帯の抽出・調整・分類を丸め前の1分単位時間で行い、その後に案件条件の時間丸めを適用する。
* **料金区分と深夜料金の重複（2026-08-31）**: 日報の各作業明細行では、通常、土曜、日祝、研修、スポット、任意料金等から料金区分を1つ選択し、その料金区分内に設定された「基本・超過・深夜・深夜超過」の単価・計算条件を使用する。例えば日祝の深夜勤務には日祝料金区分の深夜単価を適用し、日祝料金と通常料金区分の深夜料金を自動で重複加算しない。曜日・休日等から自動選択された料金区分は日報上で手動変更できる。
* **祝日・案件独自休日の判定（2026-09-01）**: 休日マスターには全案件共通の祝日と案件独自休日を登録する。登録日は実際の曜日より休日判定を優先し、同じ日に全案件共通と案件独自の登録が重なった場合は案件独自の登録を判定根拠として表示する。ただし適用する料金区分は1つだけとし、休日料金を重複加算しない。日報で料金区分を手動変更した場合は、休日による自動候補より手動選択を優先する。
* **日報計算の版管理・監査（2026-08-31）**: 下書き中の自動計算結果は最新状態へ更新し、自動再計算の全世代は保存しない。一方、単価等の一時変更、深夜帯内休憩、深夜時間調整、料金区分の手動変更等の手動操作は操作履歴へ保存する。日次確認時に、その日の全作業明細、入力値、適用契約・PriceSet・料金行、請求・支払別の計算条件、分類・丸め前後の時間と金額、手動調整を版付きスナップショットとして保存する。確認解除後に修正して再確認した場合は新版を作り、過去の確認済み版を削除しない。案件×対象月の承認時には、対象月全体を承認時点の確定スナップショットとして保存する。
* **拘束・実働と時間調整（2026-08-28）**: 拘束時間は`終了時刻－開始時刻`、実働時間は`拘束時間－休憩時間`で自動計算し、自動計算結果を直接上書きしない。途中抜け等は、正負の時間調整と理由を別項目へ入力し、自動計算値と調整内容を追跡可能にする。
* **超過時間の計算単位（2026-08-28）**: 既定は日次計算とし、日ごとに案件の基準時間を超えた分を超過時間とする。契約条件によって月次計算へ切り替えられるようにし、日報入力者が日ごとに方式を選択する運用にはしない。
* **不足時間と不足控除（2026-08-29、2026-09-01補足）**: 日次基準時間に満たない時間を不足時間として控除する。不足時間は`案件の日次基準時間－調整後計算対象時間`が正の場合に日次で算出し、不足控除額は不足時間へ時間単価を掛けて求める。請求側と支払側では、基準時間、時間単価、時間・金額の丸め条件を独立して設定し、適用条件と丸め前後の控除額を追跡可能にする。開始・終了が未入力の下書きでは不足を計上せず、欠勤日は開始・終了値の有無にかかわらず通常勤務と不足控除の計算対象外とする。
* **深夜計算の分離（2026-08-29）**: 深夜時間帯、深夜超過、通常超過との重複加算、専用単価、丸めはIssue #28で確定する。確定前に一般的な時間帯や現行コードの挙動を正式仕様として実装へ固定せず、この論点はD-01の完了条件から分離する。
* **料金マトリクスの自動計算と利益率警告（2026-09-02）**: 金額データでは料金項目ごとに、請求・支払を各側の日額基本単価と日次基準時間から自動計算する。時間外・深夜・深夜超過の倍率と利益率警告基準は全案件共通のシステム設定で管理し、初期値は各1.25、1.35、1.6、10%とする。金額は整数円で扱い、端数は四捨五入する。距離超過・スポットは自動計算しない。
* **深夜時間帯（2026-08-30）**: 深夜時間帯は請求側・支払側ごとに案件条件で開始・終了時刻を複数設定可能とする。新規案件には勤務日基準の`22:00～29:00`（翌朝5時）を1区間だけ初期登録し、両側とも変更・追加できるようにする。勤務が24時間を超える場合は、各深夜帯を24時間ごとに繰り返して勤務区間との重なりを判定する。例えば`20:00～47:00`では、初期値に対して`22:00～29:00`と`46:00～47:00`が候補となる。深夜計算区分が「対象外」の側では判定しない。
* **深夜料金を別途計算しない契約（2026-08-30）**: 基本業務が深夜で、深夜料金が通常料金へ含まれている契約等を扱う。請求側・支払側ごとに深夜計算区分として「別途計算」「基本料金に含む」「対象外」を設定可能とする。「基本料金に含む」では深夜対象時間を社内の計算根拠として計算・保存するが、別料金を加算せず外部帳票へ深夜明細を表示しない。「対象外」では深夜時間自体を計算・保存・表示しない。
* **距離超過の契約別計算（2026-08-28）**: 距離超過は契約ごとに、日次の基準距離超過、月間累計の基準距離超過、距離帯の下限・上限と適用値を持つ段階テーブル計算を切り替えられるようにする。段階テーブルは、該当段階の固定金額、該当段階単価を全距離または超過距離へ乗算、各段階の単価を段階内距離へ掛ける累積計算の全方式に対応する。日報入力者が方式を選択する運用にはせず、適用した契約条件、計算型、対象距離、段階、数量、単価、金額を計算結果から追跡可能にする。
* **距離超過の請求・支払分離（2026-08-29）**: 同じ案件でも、企業への請求とパートナーへの支払で、距離の計算方式、基準距離、対象距離、段階テーブル、固定額・単価が異なる場合がある。請求側と支払側のルールは独立して設定・適用し、一方の設定を他方へ強制的に共用しない。
* **距離の入力方式・単位（2026-08-29）**: 日報では走行距離を整数kmで直接入力し、開始・終了メーターは使用せず、メーター差による自動算出は行わない。基準距離と超過距離も整数kmで設定・計算し、小数距離は扱わない。月間累計方式では日別の走行距離入力を月内集計する。距離数量に対する小数丸めは不要とする。
* **距離段階テーブルの境界（2026-08-29）**: 段階テーブルは上限距離を昇順に設定し、最後に上限なしの「それ以上」を置く形式とする。整数kmのため、前段階の上限＋1kmから次の上限までを自動的に次の範囲とし、最終段階は最後の上限＋1km以上とする。上限なしは最終行に1件だけ設定し、段階の重複と空白を作らない。
* **距離計算金額の丸め（2026-08-29）**: 丸め方法、丸め単位、丸める段階は契約条件で指定する。切り捨て、四捨五入、切り上げを扱い、日別・段階別明細ごとの丸めと月間合計後の丸めを選択可能にする。請求側と支払側には異なる丸め条件を設定でき、適用条件と丸め前後の金額を追跡可能にする。
* **高速・駐車・交通費（2026-08-29）**: 日ごとに実際に発生した金額を入力する。請求側と支払側ごとに対象外、実費、固定額、上限付き実費を独立して設定し、税区分は契約または経費種別から適用する。実額、適用条件、上限、請求計上額、支払計上額、税区分を追跡可能にする。
* **その他経費と領収書（2026-08-29）**: 経費種別をマスタ管理し、日報では種別、金額、説明、領収書の画像またはPDFを明細入力する。領収書の必須／任意は経費種別または契約ごとに設定し、請求・支払への反映条件は両側で独立管理する。承認・確定後の通常編集では添付を物理削除せず、無効化・差替えとして元ファイルと変更履歴を残す。保存期限による月末確認削除は別の保管ライフサイクルとして扱う。
* **案件・パートナー・日報作業行の関係（2026-08-28）**: 1つの個別案件に割り当てるパートナーは1人とし、同じ案件へ複数パートナーを割り当てない。同じパートナーは同じ期間の複数個別案件を担当できるが、日報入力、日次確認、月次承認は各案件×対象月で分ける。同じ企業の基本案件から複数の個別案件を作り、各案件へそれぞれパートナーを割り当てることもできる。同じ案件・パートナー・勤務日には複数の作業があり得るため、日報は1日の配下に複数の作業明細行を持てるようにし、日次確認チェックはその日の全明細を対象とする。
* **出勤日数の計数単位（2026-08-29）**: 案件条件で「同日の複数作業を合計1日とする日付単位」または「各作業行を1回とする作業行単位」を選択し、日報入力者の手入力ではなく適用条件から自動計数して根拠を残す。現行運用では半日を0.5日とする案件はないため、出勤日数は整数のみとし、0.5日入力は有効化しない。ただし将来発生する可能性があるため、DB・API・計算処理は小数数量を追加できる構造とする。
* **非稼働・欠勤・研修（2026-08-29）**: 「不要」と「非稼働」は同一区分とし、欠勤は別区分にする。欠勤日は出勤日数に含めず、通常の不足時間控除も行わない。欠勤ペナルティが必要な場合は汎用の負額調整明細を使う。研修日は研修日の出勤として数え、日報で研修専用料金区分を選択し、通常出勤とは別に集計する。
* **24時超えの勤務時刻（2026-08-29）**: 24時を超える勤務は勤務日を基準とする48時間表記で入力し、例えば翌日4時を`28:00`とする。最大値は`47:59`とし、48時間以上の連続勤務は扱わない。
* **日報の料金区分・作業料金（2026-08-29）**: 案件の日極・月極・時間給等の初回料金区分は日報入力時の初期値とし、実際の計算は作業明細行ごとに選択した料金区分を根拠とする。同じ日でも行ごとに異なる区分を使用できる。月極は月極の作業明細行を選択した場合だけ案件×対象月に一度計上し、重複時は警告するが登録は許可する。日ごとに按分せず、全休・契約中止等は理由付き手動調整で取り消す。日極、月極、時間給、研修、土曜、日祝、スポットに加え、任意名称の作業料金を扱う。作業名マスタは案件へ複製するひな形ではなく入力候補とし、日報入力権限者全員にマスタ選択、直接入力、その場でのマスタ追加を許可する。追加後は入力中の行へ自動紐付けし、完全一致だけでなく正規化照合、部分一致、類似度検索を組み合わせて同名・表記違い候補を検出し、候補がある場合は新規追加を許可しない。マスタでは名称を必須とし、分類、検索用略称、表示順、既定単位は任意とする。同日の同一作業を複数行に分けた登録も許可する。計算方式は固定額、単価×整数数量、時間単価×1分単位時間とし、同じ作業でも請求側と支払側で計算方式、入力単位、数量または時間、単価、丸め条件を別管理する。通常は共通入力を表示し、両側の計算方式、入力単位、数量・時間、対象可否の差異をシステムが自動判定して、差異がある場合だけ各側の入力欄を展開する。一方だけ計算対象外にもでき、社内計算明細では0円と区別して「対象外」と表示する。固定額を適用する側は数量1を自動設定し、時間単価を適用する側では時間入力を必須とする。単価は適用期間の履歴を持ち、無効化後も過去日報・帳票参照用に期限なく保持する。下書き中だけ計算に必要な数量・時間の0または空欄を許可する。
* **日報の料金区分選択（2026-08-30）**: 日報の作業明細行へ料金区分を設け、案件の料金設定に登録された料金名から選択する。勤務日、曜日、祝日、案件独自休日、研修等の条件から初期候補を自動選択するが、日報入力者は手動変更できる。画面には自動／手動の別を表示し、料金設定ID・表示名・適用単価、自動候補、最終選択、選択元、変更者・日時を保存して過去日報の根拠を維持する。
* **作業名マスタの類似候補検索（2026-08-30）**: 空白・全角半角・大文字小文字・ひらがなとカタカナ等の正規化、部分一致、編集距離を含む類似度検索を組み合わせる。類似候補を先に提示し、候補と別作業であることを入力者が明示確認した場合だけ新規追加を許可する。既存候補の選択、新規登録、別作業としての確認は操作履歴へ保存する。2026-08-29決定の「候補がある場合は新規追加を許可しない」という部分は本決定で置き換える。
* **請求・支払入力欄の自動統合（2026-08-29、2026-08-30追記）**: 差異の自動判定で別表示した請求側・支払側の入力欄は、両側の計算方式、入力単位、数量・時間、対象可否が再び同一になった場合、値を失わず共通入力欄へ自動的に戻す。自動展開・自動統合とも操作履歴へ保存し、切替を発生させた利用者、日時、切替前後の表示状態、判定根拠となった差異を追跡可能にする。保存期間はシステムの一般的な操作履歴と同じ設定を適用する。
* **休日区分（2026-08-29）**: 祝日登録日は実際の曜日にかかわらず祝日扱いとする。案件独自休日も休日扱いとし、土日祝と重なっても休日料金を重複加算しない。
* **入力元と原本保存（2026-08-29）**: 初回入力元は画面入力、FAX、Excel取込、OCR、Web、メール、その他とし、後の修正で上書きしない。修正方法・修正者・修正日時も履歴保存する。原本・添付はNASへ保存し、管理者、事務員、承認担当者が閲覧でき、パートナーは閲覧できない。初期保存期間は3年で管理設定から変更可能とし、月末処理で管理者または事務員が削除対象を確認後に本体を削除する。見送った対象は翌月へ繰り越す。削除後もメタデータを残し、すでに削除された本体は後から期間を延長しても復元しない。
* **日報データ取り込みの初期方式（2026-09-05、Issue #58）**: Excel、メール、FAXは共通の取込バッチと確認画面を使用し、原本、解析値、確認後の値、反映先日報を追跡する。初期版は手動アップロードとし、Excelは`.xlsx`と`.csv`を対象に、列マッピング、案件照合、入力検証、重複確認後に下書き日報へ反映する。外部金額は日報の正とせず既存計算エンジンで再計算し、確認済み・承認済み・精算処理済み日報は上書きしない。メール受信箱・FAXサービスへの直接接続と完全自動登録は後続判断とする。
* **確定情報の維持（2026-08-29）**: 日報承認時に日報内容と計算根拠を固定し、請求・支払確定時に名称、金額、車両番号、帳票表示等を別途固定する。確定後にマスタを変更しても過去の日報・計算結果・帳票を変えず、再発行時も当時の内容を使用する。車両の日別・代車等の厳密な管理機能は設けない。
* **時間・金額の丸め（2026-08-29、2026-08-30追記）**: 時間の丸め単位は案件条件で1分、5分、10分、15分、30分等から選択可能にする。時間と金額の丸め段階は、それぞれ明細ごと、日ごと、月合計後から案件条件で選択する。金額は請求側と支払側で別々に、切り捨て、四捨五入、切り上げを設定できる。新規案件には時間丸め単位15分、時間丸め段階「日ごと」、金額丸め段階「明細ごと」を自動設定し、初期の丸め方式は時間・金額とも切り捨てとする。丸め単位・方式・適用段階は案件条件で変更可能とする。税額も3方式から選択し、税率別の帳票合計へ適用する。適用した方式、単位、段階と丸め前後の値を追跡可能にする。
* **備考と承認済み訂正（2026-08-29）**: 行備考はその日の作業内容・補足、日報全体メモは月全体の連絡事項、差戻し理由は承認担当者の指摘として分離する。承認済み日報は管理者または承認担当者が理由必須で承認を取り消し、事務員が修正して再申請する。請求・支払確定後も確定解除して日報を修正し、再計算・再承認・再確定する。未発行帳票は取消帳票として残さず、発行済みの請求書、請求取纏書、支払明細書、給与明細書だけを「取消済み」で保持する。取消済み帳票は発行画面で選択でき、画面・印刷物へ「取消済み（社内用）」の透かしを付け、管理者、事務員、承認担当者だけが表示・印刷できる。取消理由・取消者・取消日時は社内履歴だけに保存する。再確定後の帳票は新しい番号を採番し、取消済み番号を再利用しない。未振込・未入金の入出金予定は自動取消し、再確定時には復活させず新しい予定を作成する。新旧予定の専用相互リンクは設けない。
* **負額調整（2026-08-29）**: 欠勤ペナルティ等のマイナス調整では理由入力を必須とするが、調整明細単位の個別承認は必須にしない。入力者、入力日時、金額、理由を必ず履歴保存し、案件×月の全体承認に含める。
* **日報関連4帳票と送付状（2026-08-29、2026-08-30追記）**: 正式名称は請求書、請求取纏書、支払明細書、給与明細書とし、現行資料の「まとめ請求書」は請求取纏書に対応する。初期版では現行Excelと同じ必須表示項目・表示順を維持する。支払明細書下部の作業料金請求書エリアは同一伝票として扱う。案件の「給与明細必要」が有効な場合は、支払明細書と給与明細書の両方を作成する。0円の料金行は表示せず、調整理由は社内履歴だけに保持する。外部表示が必要な場合は帳票備考へ手動記載する。発行済みPDFを版管理し、請求書発行画面で新規発行、最新版の再印刷、新版発行、旧版の表示・印刷を選択できるようにする。送付状もシステムから作成し、宛先、住所、発行日、件名、本文、差出人、同封物名・部数、備考を編集して他の書類送付にも使える汎用付随文書とする。管理者は設定画面で送付状テンプレートを追加・編集・無効化できる。企業またはパートナーの選択時にマスタから住所・宛先を自動入力するが、発行前の手動変更を許可し、マスタへ自動反映しない。過去の送付状をコピーして新しい下書きにでき、コピー元は変更しない。発行済み送付状は送付先区分・ID、発行時点の住所・宛先、テンプレート内容、編集後の表示内容を保持し、マスタやテンプレートの後日変更・無効化で変えない。発行済みPDFと版履歴を保存し、同じ内容の再印刷では版を増やさず、内容変更後の再発行だけを新版として残す。
* **匿名化帳票ブック・PDFの照合（2026-08-29）**: `BOOKイメージ.xlsx` は4帳票の初期レイアウト、表示項目、表示順、代表的な明細構成を確認する資料として使用する。外部ブック参照が2系統残り、`#REF!`、`#NAME?`、`#VALUE!` となる式があるため、金額・日付・休日・例外計算の正解資料にはしない。計算仕様は合意済み仕様文書を正本とする。匿名化PDF `印刷帳票イメージ.pdf` は全60ページがA4で、4帳票、送付状、代表的な日報・計算DBの印刷例を含み、ページサイズ、余白、縮尺、改ページを含む初期実装の印刷基準として十分である。現行Excelのセル位置との完全一致は求めず、PDF上の読みやすさ、必須表示項目、表示順、帳票間の対応を維持する。実ファイルはリポジトリへ追加しない。
* **原本・添付のバックアップ（2026-08-29）**: NASへ保存する原本・添付ファイルは、Links-Systemが実施する通常バックアップの対象に含めない。この制約と削除後に復元できないことを、設定画面と削除確認画面へ注意表示する。
* **日報の日次確認と月次承認（2026-08-28）**: 日報の承認単位は**案件×対象月の全体**とする。日ごとのデータには日報入力者用の確認チェックを設け、原則として対象月のすべての日のチェック完了後に月次の承認依頼を行う。未チェックが残る場合は確認ダイアログで対象日を警告し、明示的な続行操作があれば理由入力なしで承認依頼を許可する。続行時は申請者・日時・未チェック日を履歴に残し、未チェックだけで申請を禁止しない。日次確認済みの間は金額計算に関係する項目を編集不可とし、修正時は入力者がチェックを外して編集・再確認する。チェックは月次の承認依頼前ならすぐ付け外しでき、備考等の非計算項目は確認済みでも入力可能とする。確認済みでも編集可能な項目は「システム全体の既定＋案件別上書き」で設定し、管理者またはシステム担当者だけが変更できる。既定では備考のみ編集可、金額関連項目は編集不可とする。
* **日報入力UIと一時単価表示（2026-09-02）**: 開始・終了・休憩は`00:00`相当、距離は4桁、経費・単価は6桁相当の入力幅とし、時間欄には案件基本値のダブルクリック反映と、料金設定の時間丸め単位（請求優先、支払、5分の順で補完）による増減ボタンを設ける。距離は1、通行料・駐車料・交通費は100ずつ増減する。状態表示のクリックで当該日の全明細を確認／確認解除する。契約単価は「基本・不足・超過・深夜・深夜超過」の横並び、請求・支払の2行とし、通常は空欄の一時変更入力と灰色の元単価参照を表示する。
* **日報表示設定と曜日色（2026-09-02）**: 一覧と詳細の入力文字を共通サイズにし、非稼働列は「不要」と表示する。土曜は薄い青、日曜・祝日・案件休日は薄い赤を既定色として行全体へ適用する。入力文字サイズ、元単価参照色、曜日の背景・文字色、代替時間刻み、距離・経費の増減単位は全案件共通のシステム設定から変更可能にし、休日の日付と案件範囲は祝日・案件休日マスターを参照する。
* **日報の未入力表示・時刻変換・保存位置維持（2026-09-02）**: 未入力欄の文字色を一覧・詳細・休憩へ共通適用し、色設定は色見本とカラーコードを併記する。元単価の参照説明は通常色とする。`8.00`や全角入力を`08:00`へ正規化し、一覧末尾へ日別請求・支払合計を追加して狭い画面では非表示にする。一括保存では月全体を再描画せず、操作位置と詳細展開を維持する。
* **金額データ入力画面の配色・操作（2026-09-10）**: 曜日ボタン（月～金、土、日祝、休）と請求額・支払額の文字色はシステム設定から変更可能にする。既定の請求額文字は濃い青、支払額文字は濃い橙とし、セル背景には適用しない。料金セルは `Ctrl`＋カーソルキーで移動でき、保存後は一覧へ戻らず編集画面を維持する。
* **日報料金種類の即時再計算（2026-09-02）**: 料金種類を変更した時点で、DBへ保存せずに選択料金の単価・条件による請求額と支払額を再計算し、行の計算結果と日別合計へ反映する。変更前料金に対する一時単価変更は新料金へ引き継がない。
* **日次確認切替時の操作位置維持（2026-09-02）**: 下書き・確認済みの切替後は月全体を再取得せず、該当日の状態だけを更新する。詳細展開、縦横スクロール、操作位置を維持する。
* **日報の月次承認と差戻し（2026-08-28）**: 承認担当者は案件×対象月の全体を確認し、問題がなければ全体承認する。問題がある場合は問題日の行へ問題チェックフラグを付け、相違内容を備考へ記載するか該当項目へ直接入力して、月次全体を差し戻す。差戻し後は事務員が内容を修正・確認し、直接入力または変更された日の確認チェックを付け直して再申請する。事務員は修正後に問題を対応済みとし、再確認した承認担当者が解消を確認してフラグを解除する。日別の部分承認にはしない。承認担当者による直接入力の変更前後の値と、問題の指摘・対応・解消確認の担当者・日時は、フラグ解除後も履歴として残す。
* **前払と分割返済の分離（2026-08-26）**: 旧記録の「分割払い（先払い）」は廃止する。前払は**案件に紐づく**、前払可能設定と出勤日数に基づく正式支払の先払いである。分割返済は**パートナーに紐づく**、会社立替の元金・利子/手数料・返済履歴・残高を管理する債権である。双方を同じテーブルや項目として実装しない。詳細は `09_日報・精算・帳票_業務要件.md`。
* **単価と条件の改定対応**: 単一レコードの上書きではなく、「適用開始日・終了日」を持たせた履歴で管理する。**現仕様（2026）**: 金額は **`price_sets`（PriceSet）** を正とし、`project_revisions` はレガシー。画面は `05_project_price_relationship.md` / `個別機能仕様書：1-3`。
* **支払帳票の出力形式**: パートナー向け支払書は別々に発行するのではなく、「1枚の用紙の上下で分割」し、実務に即した控除額と最終振込額が一目でわかる仕様とする。
* **実行基盤（2026-07-30）**: 実機は **QNAP TS-464 / QTS 5.2.9.3499**（CPU: Intel Celeron N5105 / x86_64、メモリ8GB）。Container Station（Docker Compose）で稼働。アクセスは `http://<NASのIP>:8080`。※当初Asustor想定だったが実機確認によりQNAPへ訂正。
* **認証（2026-07-30）**: 簡易ID/パスワード。
* **ログイン権限（2026-07-31）**: 正式仕様は `Login.md`。権限は複数付与可（管理者/システム担当者/経営者/総務/営業/パートナー/企業）。ユーザー項目は No, ID, 名, 権限, パスワード, 所属部署, 所属エリア など。
* **仕様管理ルール（2026-07-31）**: 仕様の正式参照元は `仕様MD/`。実装・調査時は必ず参照し、仕様変更時は該当MDを同時更新する。ログインは特に `Login.md` を正とする。実装計画は `仕様MD/計画/` に保管する。
* **仮組先行（2026-07-31）**: 今後の機能は「仮組（機能一式・DB・画面を動かして確認）→ 見直し → 本作成」で進める。詳細は `計画/00_仮組先行の進め方.md`。
* **全機能仮組（2026-07-31）**: ランチャー全機能（企業/パートナー/案件/日報/先払い/請求/支払/ユーザー）を仮組として一通り実装。PDF・本計算は本作成。詳細は `計画/04_全機能仮組.md`。
* **画面差分チェック（2026-07-31）**: `画面一覧/` の画像と現行仕様・仮組・ヒアリングの差分を `計画/05_画面差分_要不要チェック.md` に洗い出し。ユーザーが要不要を記入後、機能追加仕様へ取り込む。
* **機能追加仕様取り込み（2026-07-31）**: チェック結果を `計画/06_機能追加仕様_画面差分取り込み.md` に定着。ランチャー維持、共通一覧強化、企業／案件／日報／精算／金額マスタ等を要。サイドバー移行・ダッシュボード等は不要または保留。G-07は請求確定解除による再編集。実装は指示後。
* **機能追加仕様の仮組（2026-07-31）**: `計画/06` の要項目を仮組実装。共通一覧（A-04〜A-08/A-10/A-11）、企業・パートナー、案件分離、金額マスタ、日報月次グリッド、請求確定解除、支払・先払、マスター設定／UIビルダー。DBは `007_feature_additions_draft.sql`。次は見直し。
* **AI 開発運用（2026-08-08）**: PC Cursor（メイン実装）、PC Antigravity（仕様・検証・開発可）、モバイル Cursor Cloud（小タスク）。共通入口はリポジトリ `AGENTS.md` と `仕様MD/00_現状スナップショット.md` / `00_AI共通_開発ガイド.md`。配布正は `main` → NAS。
* **金額データの期間重複（2026-08-01）**: 重複登録を許可。日報は `apply_start_date` が勤務日以前で最大のセットを適用。改定はコピー API＋UI で前回単価から開始日・金額を変更。
* **環境分担（2026-07-31）**: **テスト＝ASUSTOR AS3202T（ADM 4.3 / メモリ2GB）**、**本番＝QNAP TS-464（QTS / メモリ8GB）**。流れは 開発 → Asustorテスト → QNAP本番。Asustor配備は `計画/07_AsustorテストNAS配備手順.md`。ソース／DB更新は `計画/08_Asustor更新・DB手順.md`。
* **通信言語・トークン節約（2026-07-31）**: 最終返答／コミット／PR／仕様MDのユーザー向け文のみ日本語。Thought／作業途中の説明は英語・短文。Cursor User Rules の「すべて日本語」は最終面だけに狭める必要あり（さもなくば Thought UI が日本語のまま）。ルール本文は英語。和訳は依頼時または大幅変更時。
* **帳票（2026-07-30）**: **PDF出力を優先**。ブラウザ印刷（印刷用CSS）も併用可。
* **データ保存（2026-07-30）**: IndexedDBではなく共有MariaDBへ変更（4名同時更新のため）。詳細は `06_development_environment.md`。
* **複数AI開発運用（2026-08-26）**: Cursor、Antigravity、Codex等の共通入口を `AGENTS.md` と `00_AI共通_開発ガイド.md` に集約する。作業中の担当・編集範囲・引継ぎは、共有ロック用MarkdownではなくGitHub IssueとDraft PRを正本とする。詳細は `docs/development/AI_COLLABORATION.md`。
* **入出金管理・FBの基礎概念（2026-08-26）**: 月6回（5日/10日/15日/20日/25日/末日）の業務上の管理回と、予定日・実行日を分離する。入出金管理回、入出金予定、入出金実績の3層で設計し、回単位の日付一括変更と取引単位の例外を両立させる。詳細は `10_入出金管理・FB_業務要件.md`。
* **請求・支払本作成（2026-09-03）**: 月次承認済み日報を根拠として、請求・支払を別々の明細・調整・確定スナップショットで管理する。総務作成、案件ごとの営業確認、管理者または経営者の最終確定を必須とする。最終確定時に入出金予定と年次連番のPDF帳票を作成し、訂正では未実行予定を取消して新番号で再確定する。控除は有効期間付き共通・パートナー別ルールを正本とし、支払額を超える未控除分は繰越明細にする。事務手数料1,100円と安全協力会費8,800円は初期ルールとして移行する。
* **請求・支払の予約・取消・実行後訂正（2026-09-03）**: 下書き明細は最新の月次承認済みスナップショットから生成し、対象日報を `reserved` にして二重下書きを防止する。未実行取消では日報予約、前払配賦、繰越配賦を履歴付きで戻す。実行済み入出金を含む場合は直接取消を禁止し、訂正元IDを持つ新下書きから新番号・新PDFを発行し、同一訂正系列の実行済み総額との差額だけを予定化する。負差額は逆方向予定とする。CSV出力明細の取消状態も予定と同期する。
* **請求・支払の運用受入基準（2026-09-03、Issue #52）**: 匿名化した隔離DBで、管理者・総務・経営者・営業・企業・パートナーの権限、月次承認スナップショット、二重予約防止、税率優先、案件集約、前払・控除・0円振込・繰越、未実行取消、実行済み差額訂正、4種A4 PDF、検証データ限定リセットを自動検証する。営業は担当案件の請求・支払、企業は自社請求、パートナーは自己支払を参照できる。正式FB・承認分離・銀行明細照合は、対象銀行と運用を確定後に別Issueで実装する。
* **銀行別CSV出力と状態分離（2026-09-06、Issue #79）**: りそな銀行・埼玉りそな銀行、みずほ銀行、三井住友銀行を振込元口座側の銀行系列として扱う。列・ファイル設定は版管理されたマスターとし、公開済み版と過去出力を上書きしない。CSV出力では支払実行済みにせず「出力済み」で止め、銀行処理後の実績登録を別操作とする。正式な銀行サービス仕様が未提示の版は下書きのまま銀行取込用に公開しない。
* **請求・支払・前払画面と月額集約の是正（2026-09-03、Issue #54）**: 請求・支払の下書きは日別行ではなく、月次承認済みスナップショットを案件・表示項目・単価・税区分ごとの月額明細へ集約する。請求は案件選択式とし、同一請求先・請求取纏番号の案件だけをまとめる。下書き明細は摘要、単価、数量、金額、税区分、表示順を編集でき、金額上書き、手動追加、取消、日報差分反映は理由と変更履歴を残す。手動追加行と手動編集済み行は選択されない限り再反映で置換しない。請求・支払・前払の月選択を日報と統一し、前払は締日による3グループで支払予定を作る。日報一覧には締日、入力進捗、月次承認状態を表示し、条件で絞り込めるようにする。帳票の正本は編集済み下書きデータとし、確定PDFへ同じ内容・順序を固定する。
* **先払3サイクル・マトリクス（2026-09-05）**: 個別案件の`支払区分＝分割`を先払対象条件の正本とし、稼働0日でも一覧へ表示する。対象期間は、5日系が前月26日～当月5日／6～15日／16～25日、10日系が1～10日／11～20日／21日～末日で、支払予定日は当月20日／当月末日／翌月10日とする。画面は既存サイドバー・ヘッダー内の「案件情報／3サイクル／案件別合計」5列とし、案件数無制限の縦スクロール、固定見出し・左右列、マトリクス内横スクロールを採用する。先払フラグはサイクルごとに初期ON、OFFは対象月だけに保存する。支払額・手数料は元値を併記して手修正でき、変更理由と履歴を必須とする。手数料は共通固定金額マスターを案件、パートナー、0円の順で解決し、予定作成時にスナップショット保存する。未実行予定は理由付き作成取消で未作成へ戻して再作成可能とし、実行済みは返金・訂正予定で扱う。旧`project_advance_terms`と既存前払記録は互換データとして保持する。

* **ローカル画面の読み込み障害是正（2026-09-06、Issue #86）**: ユーザー申告のlocalhost:8080で画面JS取得が約30秒後に接続リセットとなることを再現。ローカル接続を127.0.0.1へ統一し、機能別JSを必要時に取得する。取得失敗は未実装扱いにせず理由と再試行を表示する。集計待ち中もメニューを操作可能とする。金額計算・権限・保存内容は変更しない。調査・検証・復旧手順は `計画/reviews/2026-09-06_画面読み込み障害修正.md`。
* **基本管理のミラーカラム（2026-09-08、Issue #97）**: 企業、基本案件、個別案件、金額データを5列目の詳細プレビューとともに1画面で確認する「基本管理」を追加する。通常は上位から順次選択し、企業または選択企業内の基本案件を「全対象」にすると4階層を行結合して表示する。下位データがない親も省略しない。クリックは項目別プレビュー、ダブルクリックとプレビュー内ボタンは既存編集画面への直接遷移とし、登録・保存は既存各マスター画面を正本とする。専用APIは既存4マスターの参照集約だけを行い、新しいDBテーブルや計算仕様は追加しない。利用権限は管理者、システム担当者、総務とする。詳細は `個別機能仕様書：1-4. 基本管理画面.md`。
* **基本管理の部分更新と金額プレビュー（2026-09-08、Issue #97）**: 項目タップ時は画面全体を再描画せず、選択状態、下位カラム、パンくず、プレビューだけを更新してスクロール位置を維持する。共通ヘッダー、列見出し、プレビュー見出しは固定し、各一覧とプレビュー本文を独立スクロール領域とする。金額データのプレビューには登録料金行の請求・支払単価合計、差額、参考利益率を表示し、月次請求額・支払額ではないことを明記する。
* **基本管理のスマホ・タブレット縦スクロール（2026-09-08、Issue #97）**: 横方向のミラーカラム移動とカラム内部の縦移動を分離し、通常表示の企業・基本案件・個別案件・金額データ、全対象表示、詳細プレビュー本文をそれぞれタッチ操作で最下部まで移動可能にする。高さを画面内で明示し、iOS Safari向けの慣性スクロールを有効にする。
* **基本管理の欠損からの新規登録（2026-09-10）**: 企業・基本案件・個別案件を順に選択し、次階層が0件の場合は「基本案件なし」「個別案件なし」「金額データなし」を新規登録ボタンとして表示する。通常表示と全対象表示のどちらからも、表示中の親項目を初期選択した既存の各新規登録画面へ移動する。登録と保存の正本は引き続き既存マスタ画面とする。
* **請求先No・事前下書き・状態色（2026-09-08、Issue #100）**: 請求先Noは企業内で1から自動採番し、削除済み番号を再利用しない。個別案件は同一企業の請求先を選択できる。請求・支払は日報未完了でも対象案件を保持した0円下書きを作成できるが、営業確認・最終確定は全案件の月次承認済み日報を反映した後だけ許可する。見本PDFは現在の下書き明細・合計を使い、金額明細追加は表内の新規行として行う。共通状態色は未処理・作業中・確認待ち・完了・要対応・無効の6分類を直感的な既定色で表示し、システム設定から変更できる。スマホの複数行操作は企業・パートナーと同じ「操作」メニューへまとめる。
# 2026-09-09 共通操作・契約状態・ヘルプ・帳票番号

- 全機能の上部ヘッダーは、メニュー開閉の右側に「戻る」を置き、「運送業務基幹システム」の固定文言を表示しない。戻るは画面内の直前表示へ戻り、履歴がない場合は業務ダッシュボードへ戻る。
- 企業、パートナー、基本案件は契約状況区分（稼働中／一時休止中／終了予定／終了）と稼働終了日を持つ。通常一覧は「終了」または終了日経過を除外し、「終了しているものも表示」で含める。
- 全機能は画面ヘルプを表示し、管理者・システム担当者が専用の「ヘルプ編集設定」で画面概要と「何を入力するとどうなるか」を変更できる。
- 一覧の文字フィルタは日本語IMEの変換確定前に再描画しない。
- 請求書・支払書番号は相手先IDの5桁、対象年下2桁、相手先・帳票種別・年別の2桁連番を基本形とする（例 `00012-26-01`）。99件を超えた連番は桁を切り捨てず拡張する。取消番号は再利用しない。
- 「日報から再反映」は日報の完了有無にかかわらず現時点の保存データを下書きへ反映できる。営業確認・最終確定は従来どおり全対象案件の月次承認済みを必須とする。

# 2026-09-09 請求先・取りまとめ請求・統合承認

- 請求先Noは企業内の0始まり連番とし、No.0を必須の既定請求先とする。企業登録時は企業情報からNo.0へ自動反映し、追加請求先にも企業情報をコピーできる。基本案件・個別案件は同一企業の請求先を必須選択し、初期値をNo.0とする。これはIssue #100時点の「1始まり」を置き換える決定である。
- 取りまとめ請求は、同一企業・同一対象月・同一締日・同一かつ空欄でない取纏Noの、異なる請求先Noに属する複数の通常下書きから別の請求書を作る。元下書きは履歴として保持・編集ロックし、取りまとめ請求の取消時にだけロックを解除する。
- 事務担当の最終確認後に請求・支払下書きを作り、営業確認依頼時に通常編集をロックする。案件に設定された営業担当者全員の承認を必要とし、担当者がいない場合は管理者1名を自動割当する。日報・請求・支払は案件月単位で一括承認する。上長承認要否は全社設定とし、初期値は不要とする。
- 一括承認後は修正不可とし、帳票発行を独立操作として行う。発行済み請求・支払を直す場合は理由必須の発行無効処理と訂正下書きを作り、将来の上長承認に備えて申請者・決裁者・理由・日時を保存する。
- 金額・数量入力中は入力要素を作り直さず、同一行の計算結果だけを更新する。基本管理の項目はダブルクリックまたは350ms以内のダブルタップで対応する編集画面へ移動する。一覧の複数操作は1024px以下で「操作」にまとめる。

# 2026-09-10 金額データ編集のExcel型3カードUI

- 金額データ編集は、デスクトップで料金カードを横3列に固定し、各カード内を罫線付きのExcel型入力表として表示する。4列表示にはしない。
- 基本情報は画面上部へ「適用開始、適用終了、企業、名称」の順で1行配置し、備考は画面最下部へ移す。
- 料金行の適用条件、請求計算式、支払計算式は常時展開せず、行末の「適用条件」ボタンから展開する。管理者以外には式を開示しない。
- 勤務・深夜・丸め条件は同じ保存項目を維持したまま4列の高密度配置とし、通常入力欄を従来の約1/2、日次基準時間を約1/4相当に縮めて画面高さを抑える。
- タブレット・スマートフォン（1100px以下）は料金カードを1列の横長配置に切り替え、料金行の追加・削除ボタンを非表示にする。カード右上の優先順位・コピー・削除はアイコン表示とし、マウスオーバーで機能名を表示する。コピーは黄色、削除は赤色とする。
- 選択済み曜日は、月〜金を濃い緑、土を濃い青、日・祝を濃い薄赤、案件休日を濃い赤で表示する。

# 2026-09-10 利用者向け動的HTMLマニュアル

- 利用者向けマニュアルの正本はリポジトリ直下の `利用マニュアル/` とし、稼働中のシステムでは社内LAN内の `/manual/` でログイン前から閲覧できるようにする。掲載画像は匿名の検証データに限定する。
- 共通ヘッダーの「ヘルプ」横に「利用マニュアル」を全ロール共通で表示し、現在の業務画面を維持したまま新しいタブで開く。画面ヘルプは現在画面の短い説明、利用マニュアルは業務全体・前後関係・具体例を説明する資料として分離する。
- マニュアルは実装済み全18機能を対象に、各機能へ目的、利用担当、画面イメージ、具体例3件、前後の機能、重複して見える機能との使い分けを掲載する。
- 業務フローの関連強調、画像拡大、URLハッシュによる章参照を動的機能とし、JavaScriptが無効でも本文を読めるHTMLにする。A4の章別印刷と全体印刷の両方を提供する。

# 2026-09-10 日報起点の検証データ作成ツール（Issue #117）

- ユーザー合意計画により、検証環境限定の管理者機能として実装する。通常業務DBの置換と本番実行は対象外。
- 初回はExcel/CSVの列割当、名称の維持/仮想化、件数・期間・日報ケース、サンプル採否・版承認・匿名共有まで。取込件数を優先し、未取込時は小規模仮想データ、期間は当月を含む直近3か月。
- 設定変更時は承認を失効させる。原本を外部サービスへ送らず、共有出力は既定で元の名称・自由コメント・接続情報を含めない。
- この段階では実績登録や正式な金額計算を行わない。独立DB生成・精算・帳票・復元は後続作業として明示する。詳細は `計画/28_日報起点検証データツール.md`。
# 2026-09-11 Excel列のクリック連携（Issue #119）

- 左に日本語の取込項目、右にExcel列名と先頭2データ行を表示し、どちらから選択しても連携できるようにする。Excel側のシート切替で各シートの対応付けを保持する。
- 企業の担当・形態・検索用等も検証データ設定に保持する。営業担当マスター登録や形態コードへの変換、業務DB登録を行ったものとは扱わない。
- 連携解除、列の重複割当確認、対象外シート選択を設ける。対応付け変更後は再反映・再プレビューを要求し、以前のサンプルや匿名共有表示を無効にする。
- 取込先はシート名・列名から初期判定する。保存時に未反映の列連携を自動反映し、未連携列は件数を明示して除外する。コードがない人物等は `P00001` 形式で仮想補完する。
- シート対象指定とサンプル必須指定、仮想補完許可は目的を文章で示す。列連携行はスクロール可能なコンパクト表示とする。
- 1シートに複数の取込先設定を保持し、切替時に既存割当を消さない。取込項目はデスクトップで3列表示し、割当操作後も左右リストのスクロール位置を維持する。
- 重複番号は取込先ごとにエラー、先行優先、後行優先、数値平均の切捨て／切上げを選択できる。平均対象は業務上の数値項目に限定し、口座番号等の識別子は平均しない。
- 取込項目の3列化は取りやめ、左の取込項目を縦並び、右のExcel列を縦並びとする。3列表示は通常勤務・早朝勤務等の稼働パターン設定カードへ適用する。

# 2026-09-12: 編集用マスターExcelとDB取込

- 旧Excelの原本値はサーバー、DB、ログへ永続保存せず、処理直後に削除する。
- 利用者向けExcelでは内部ID/FKを公開せず、安定した取込キーで関連を解決する。
- `★自動補完` はExcel数式ではなく再取込時にサーバーで再計算し、利用者が入力した値は採用しない。
- 更新対象は本機能の取込キーマッピングがあるレコードだけとし、名称一致だけで手入力データを更新しない。
- 正常行だけを依存順に1トランザクションで保存する。親エラー、競合、予期しないDBエラーの扱いは計画書に従う。
- 支払月オフセットなど保存先がない業務項目はExcelに残すが、本機能ではDBへ保存しない。

## 2026-09-12: 登録済みマスターの日報サンプル連携

- 編集用マスターExcelを専用検証DBへ登録した後、検証データ作成画面から登録履歴を読み込み、企業・パートナー・基本案件・個別案件の件数・名称・参照関係を再利用する。
- 画面上の識別番号は登録順に `C00001`、`P00001`、`B00001`、`J00001` として付与し、DB内部IDは変更しない。
- 同一パートナーの複数案件配属は許可する。日報サンプルでは同じ日に稼働する案件を1件に限定して勤務重複を防ぐ。
- 元Excel、接続情報、DB内部IDと取込キーは匿名共有パッケージへ含めない。

## 2026-09-12: 承認済みサンプルの日報生成（Issue #126）

- 承認済み設定からの初回業務生成は、専用検証DBの日報までを対象とする。月次承認、先払、請求、支払、入出金、帳票は日報生成の検証後に接続する。
- 生成ジョブは設定IDと版で一意にし、100件単位で確定する。中断時は生成済み対応表を照合して同じジョブを再開し、日報を二重生成しない。
- 完了予定月は日次確認済み、処理中月は確認済みと下書きを混在、当月は下書きとする。欠勤と勤務不要は時刻・距離・金額がない日報として区別して保持する。
- 日報は現行料金計算を利用し、生成後に件数、未来日、金額未計算、稼働日の0円、同一人物同日の稼働重複を検証する。企業・パートナー・基本案件・個別案件の有効期間外は勤務不要とし、稼働実績を作らない。
- 初期のマスターExcelが基本料金コードを `base` で作成していた不整合は、現行のコードマスターと計算エンジンに合わせて `basic` へ正規化する。旧取込データも新規マイグレーションで同じコードへ変換する。

## 2026-09-12: 日報提出・月次承認の検証データ生成（Issue #128）

- 日報生成が完了した設定版を入力として、案件月ごとに締日別3サイクルの日報提出を作成する。完了月は原則月次承認済み、前月は承認済み・承認待ち・差戻し・未申請を混在、当月は提出途中とし月次承認を作らない。
- 提出日・承認日は基準日を超えて生成しない。前月の提出遅延は異なるパートナー5名へ固定的に割り当て、同じ設定と乱数シードで再現できるようにする。
- 取込案件の締日が空欄または不正な場合は、通常DBへ波及しない専用検証DBに限り6種類の締日を決定的に補完し、元値と生成値を生成ジョブへ紐づけて記録する。
- 同じ案件月に既存の月次承認がある場合は削除・上書き・生成物への付け替えを行わず、既存データとしてマニフェストへ記録して残りの生成を継続する。

## 2026-09-12: 先払・請求・支払の検証データ生成（Issue #130）

- 月次生成ジョブが作った承認済み案件月だけから、現行の承認スナップショットと精算明細計算を利用して請求・支払を生成する。未承認・差戻し・当月入力途中は精算対象にしない。
- 完了月は承認済み、前月は下書き・営業確認依頼・営業確認済みを混在させる。請求と支払には各6件の理由付き手入力調整を含める。
- 分割案件から異なる30名を選び、3サイクルの先払を生成する。実行済み・保留・取消を混在させ、実行済み前払だけが後続の正式支払確定で控除候補となる。
- 本段階では請求・支払を最終確定しない。PDF、通常支払の入出金予定・実績、銀行CSV、正式FBは次段で生成し、未対応状態を完了として表示しない。
- 生成ジョブと生成物対応表を追加し、同じ月次ジョブの再指定および障害後再開で先払・請求・支払を二重作成しない。
- 生成物は専用対応表で追跡し、失敗後の再開で日報提出・月次承認・営業確認ワークフローを二重生成しない。精算、先払、請求、支払、入出金、帳票は後続段階で接続する。

## 2026-09-12: 確定・帳票・入出金・検証専用銀行CSV（Issue #132）

- 承認済みの検証用請求・支払は、通常APIと同じ最終確定関数を使用し、税計算、先払・手数料控除、確定スナップショット、帳票採番、入出金予定を作る。生成側に別の確定計算を持たせない。
- 完了月は原則入出金実績まで作り、一定間隔で意図した未完了を残す。CSV出力済みと実行済みは別状態とし、実績作成前のCSV内容・定義版・件数・合計・SHA-256を保持する。
- 公開可能な銀行CSV定義は `verification_` 接頭辞と「検証専用」の表示名を持つ3件だけを生成する。架空口座と匿名化した振込先を使用し、実銀行向け定義、正式FB、銀行送信済みとして扱わない。
- PDF実ファイル、DB帳票参照、先払配賦、入出金実績日、CSVチェックサムを出力ジョブの完了時に照合する。不整合があれば完了にせず、対応表から再開する。
## 2026-09-12: 基盤マスター初期値のExcel正本（Issue #135）

- 初期値は版管理した `backend/foundation_masters.xlsx` を正本とする。2行目の項目コードと安定キーで起動時に検証し、不足行だけ追加する。既存環境で変更した値はExcel変更後も上書きしない。
- Excelに個人情報や実口座を含めない。設定追加時はファイル変更をレビューしてから通常の更新手順で適用する。未対応の列・式・欠損シートは起動失敗として扱う。

## 2026-09-12: 計算ルールの税率表示と再計算の整合（Issue #136）

- 請求税率と端数方式は請求先・案件・システム設定を優先する既存仕様を維持する。計算ルールの内部既定値を画面上で変更可能な業務税率として扱わず、選択再計算と最終確定で同じ税率解決を使う。
- 現行版との比較は標準例による比較であり、実際の請求・支払全件を検証したものとして表示しない。

## 2026-09-13: 計算ルールの必須処理と確定時の金額検証（Issue #136）

- 支払側の最終金額処理を外しても公開前検証を通過し、確定時に未計算値が0円へ読み替わる不備を防ぐ。請求・支払ごとに月次集約、控除または税、最終金額の条件なし処理を必須とし、処理種別と段階の不一致を拒否する。
- 実行時も必須の金額が欠落・非有限値なら保存を中止し、精算のトランザクションをロールバックする。正常な0円結果は維持する。
