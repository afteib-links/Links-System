# 画面点検・改訂検証記録

2026-09-25。関連仕様: [改訂計画](31_事務入力改善・OCR・燃料費・年度締め.md)。未検証を合格として扱わない。

## 現状確認済み

- 日報対象一覧: 締日未設定が「-日」、対象名が入力ヘッダーで不足。カレンダー月と締め期間が不一致。
- 日報入力: 1912pxでも横スクロール。時間ボタンがTab対象、230不対応、詳細の時間正規化が未統一。不正値はalert。料金説明にdaily/hourlyが露出。
- 日報詳細: 料金・計算根拠が常時展開され通常入力を圧迫。
- データ取込: Excel/CSVのみ、新規INSERTのみ。PDF原本比較・既存値の項目別再取込なし。
- 基本管理: 初期余白、絞込操作「抽」の説明不足。
- 事務作業: 企業列の絞込ボタンの欠け（元worktreeに別作業の変更があり本作業には混ぜない）。

## 点検継続対象

認証/ホーム、企業/拠点/パートナー、基本/個別案件、料金、日報提出、事務作業、請求/支払、先払、入出金、収支、マスター/権限/ヘルプ。検索保持・表示密度・ラベル・金額表示・PC/スマホ主要導線を各画面で確認する。

## 検証結果

実装とともに、実施コマンド・結果・未確認を追記する。

## 2026-09-25 日報の入力効率改善（Issue #160）

時間優先表示/全項目表示、共通時間パーサー、時分選択、入力欄だけのTab、F2/Ctrl+S、詳細の区分、整数円検証、小数既存単価保全、保存競合時の追加入力保持を実装。input-defaults APIに会社/パートナー/案件表示名を追加（既存レスポンス互換）。DB migration・計算方式の変更なし。

検証:
- node --test backend/test/*.test.js: 261件合格。
- daily_entry_efficiency.js: 20日Tab/逆Tab、全角/230/28:00/不正値、時分選択、詳細、整数、入力保持、保存競合、1920/1366/390pxを合格。
- daily_report_ui.js: 匿名MariaDBで単価変更、理由、日次確認/解除、同日複数作業、深夜共通/分離、月次警告を合格。
- 別Compose links-office-verify / 127.0.0.1:8087でビルドとhealth db upを確認。通常8080/3306の既存DB・アプリは更新していない。

未完了: 全画面点検、締め期間移行、PDF OCR/選択再取込、追加項目/燃料費、年度締め、NAS実機検証。日報の承認ロックと月次集計は既存仕様のまま。計画全体は未完了。

## 2026-09-26 案件の締め期間（Issue #162 / PR #174）

新規migration046で期間履歴と移行監査を追加。入力・日数・休日・未確認警告・月次スナップショット・Excel/CSV取込・精算下書き・新規帳票を同じ期間に接続。既存暦月を保持し、事務/管理者が未確定分の前後差分と理由を確認して移行できる。移行で金額を再計算しない。

検証: 単体270件、期間のAPI統合（作成、範囲外、CSV、承認、移行競合、確定保護、金額不変）、Edge入力回帰と月跨ぎ表示・理由必須、1920/1366/390pxに合格。匿名Dockerビルドと8087のhealth db up、既存精算フロー（スナップショット、予約、税、0円、取消、訂正）に合格。設計書のAPI/DB構造を再生成して整合確認。既存DB取込・検証生成の旧形式日報にも暦月互換を適用する。

通常利用中の8080と実DBは更新していない。残り: PDF OCR/選択再取込、追加項目/燃料、年度締め、全画面の最終点検、NAS実機・手書き実用精度の測定。

## 2026-09-26 PDF原本比較と選択取込（Issue #163）

migration047、PP-OCRv5の独立CPUワーカー、ジョブ復旧・再試行、実際の原本画像、ページ位置・列座標、様式再利用、候補と現在値の比較、項目単位の選択、理由付き再取込、版競合・確認済み・月次承認・精算ロックを実装。通常取込APIからPDF更新を迂回できないように分離した。右側の手修正を優先し、原読取値と初回入力元を保持する。

単体274件合格。匿名3行PDFの原本生成からOCR・選択反映・二重実行・競合・確認ロックまでAPI試験合格。ブラウザで既存初期OFF、項目選択、手修正優先、テンキー時刻・選択パネル・Tab、不正入力保持、1920/1366/390pxを検証。画面を画像でも確認し、チェック欄の過大表示を修正した。検証用Dockerでワーカー1GB制限・外部接続なし、解析9.52秒・最大RSS523,284KB。実手書きの精度・作業時間とNAS実機は未測定（ocr/README.md）。年度締めロックは年度機能実装時に接続する。
