# レビュー: PR #143 システム専用メニューとExcel出入

- 日付: 2026-09-13
- 対象: PR #143 / `cursor/system-menu-excel-148a` / `5414d76`
- レビュー: 独立AI（専用Bugbotはこの環境で実行不可）
- status: 完了

## 結論

初回レビューと修正後レビューで見つかった再現可能な6件は対応済み。最後に指摘された3件は `5414d76` での解消を独立に再確認した。バックエンド251件、PRのCI 4件、隔離Dockerの `/api/health`（`db: up`）は成功。専用Bugbotによるレビュー済みとは扱わない。

## 指摘と対応

- R1（高）: 基盤初期値の復旧で採番カウンターが巻き戻る。`next_number` を復旧対象外とし、欠落ルールは競合にした。通常登録とExcel取込も使用済み番号を飛ばす。
- R2（高）: メニュー権限を外しても精算・銀行設定APIを直接呼べる。請求・支払・設定のAPIに機能権限を適用した。
- R3（高）: 過去料金改定版をExcelから監査なしで更新できる。過去版とその行は変更不可にし、登録直前にも確認した。
- R4（高）: 符号化URLで精算APIの権限判定を迂回できる。未認識の経路を拒否した。
- R5（高）: 現行料金行を過去版へ付け替えてR3の防止策を迂回できる。移動元と移動先の両改定版を確認した。
- R6（中）: 不正な日付が空欄として保存され、料金適用期間が無期限になる。プレビューで行エラーにした。

## 仕様MDへの反映

- `計画/30_システム専用メニューとExcel出入.md` と `05_decision_log.md` に安全条件を追記した。
