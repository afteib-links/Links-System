# レビュー: 請求・支払・企業画面のBugbot指摘対応

- status: 完了
- 日付: 2026-09-05
- 対象: `cursor/billing-payment-company-ui-148a` / `b75dc24` 後の差分
- レビュー: Codex（Bugbot相当レビュー）

## 結論

指摘5件を修正済み。追加した回帰テストを含むバックエンド単体テスト80件が成功した。

## 指摘

| ID | 重要度 | ファイル | 内容 | 対応 |
|----|--------|----------|------|------|
| R1 | 高 | `backend/src/routes/companies.js` | 画面から除外した企業直下の旧送付情報が更新時にNULLになる | payloadにない項目は既存値を維持する部分更新へ変更 |
| R2 | 高 | `backend/src/routes/settlements.js` | 摘要表示順を変えると表示名が別区分へ割り当たる | 表示順の各位置に同位置の表示名を対応付け |
| R3 | 高 | `backend/src/routes/settlements.js` | 支払見本に前払・手数料・控除・繰越が反映されない | 正式確定と共通の控除計算で見本行と振込額を生成 |
| R4 | 中 | `backend/src/routes/settlements.js` | 取纏請求の見本が通常請求書になる | 企業設定に応じて見本も`invoice_summary`を使用 |
| R5 | 中 | `backend/src/routes/payments.js` | 同一パートナーの複数案件行で控除が重複する | パートナー・締日単位で控除残額を案件行へ一度だけ配賦 |

## 仕様MD への反映

- [x] 不要（既存の `計画/19_請求支払・金額・企業画面改善.md` に沿う不具合修正）

## 検証

- `node --check`（変更した3ルート）: 成功
- `node --test test/*.test.js`: 80件成功、失敗0件
- `git diff --check`: 成功
- Docker / API確認: 実行環境にDocker実行ファイルがないため未実施
