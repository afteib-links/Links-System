---
feature_key: daily_reports
related_features: projects, price_sets, daily_report_submissions, calculation_rules, invoices, payments
primary_tables: daily_reports, daily_report_periods, daily_report_period_audits, daily_report_audit_logs, daily_report_confirmation_snapshots, daily_report_monthly_approvals, daily_report_distance_monthly_results
api_prefixes: /api/daily-reports, /api/daily-report-imports
specs: 仕様MD/個別機能仕様書：2-1. 日報登録・一覧管理画面.md, 仕様MD/04_calculation_logic.md, 仕様MD/09_日報・精算・帳票_業務要件.md
---
# 日報

追加項目は `/api/additional-items` に日別または期間別で複数保存します。直接金額・単価数量・自家用燃料費を選び、請求/支払を独立して保持します。燃料は適用日付きの全社/パートナー/案件設定と価格履歴から日額切上げを合計し、日別根拠を保存します。月次承認時に固定し、精算明細と収支分析へ接続します。税込区分は内税を分離し、外税を二重加算しません。確認/承認/精算中の変更は拒否し、既存設定変更で過去額を再計算しません。

PDF取込は `/api/daily-report-imports/pdf` で受け付け、独立したCPUワーカーが永続ジョブを処理します。実際の原本行画像・原読取値・修正値・現在値を比較し、期待する日報版と反映項目を指定して保存します。原読取と初回入力元は変更しません。空欄は削除扱いにせず、確認・月次承認・精算済みは保護します。様式設定、手入力継続、失敗再試行を提供します。構成と検証限界は `ocr/README.md` に記載しています。

## 目的と設計意図

案件ごとの勤務実績、時刻、距離、経費、休暇等を日単位で記録し、請求・支払計算の根拠にします。入力値、計算結果、承認スナップショットを分離して追跡します。

## 利用場面と操作手順

日々の勤務実績を入力し、月次の承認と請求・支払の根拠をそろえるために使います。

1. 対象月と個別案件を選び、企業・パートナー・締日が合っているか確認します。
2. 日ごとに開始・終了、合計休憩、距離、経費、必要な作業明細を入力します。翌朝までの勤務は勤務日基準で、例えば20時から翌4時を `20:00～28:00` と表します。
3. 自動選択された料金区分と請求・支払それぞれの計算結果を確認します。例外は理由を残して、画面で用意された調整操作を使います。
4. 保存後、その日の全明細を確認して日次確認を付けます。月の入力と確認を終えたら案件×月の承認を依頼します。
5. 差戻しがあれば指摘箇所を修正・再確認して再申請します。日次確認済みの計算項目を直す場合は確認を外します。月次承認済みの場合は所定の取消・訂正手順へ進みます。

## なぜこの手順で利用するか

時刻・距離は事実、料金・丸めは契約条件、承認は確認責任という役割が異なります。入力後に計算を確認するのは、同じ勤務でも請求側と支払側の条件が異なるためです。日次確認は入力者の確認であり、月次承認とは別です。後工程がどの版を根拠にしたか追えるよう、確認と承認の時点で記録を残します。

## 利用例と完了の確認

架空の案件で `09:00～18:00`、休憩 `1:00` を入力した場合、実働が8時間になることをまず確認します。請求・支払額はその案件の契約条件と照合します。入力件数だけで完了と判断せず、日次確認、月次承認、請求・支払への反映を順に確認します。未確認日が残る場合の警告は内容を確認して判断し、未確認を黙って完了扱いにしません。

## 作成・変更の経緯と根拠

`仕様MD/09_日報・精算・帳票_業務要件.md`は、実績・計算・調整・確定結果を分離し、旧Excelの数式を直接書き換える代わりに理由付き調整を残す方針を明記しています。§3.4では日次確認と案件×月の承認を分けています。詳細な計算要件には確認待ちの項目もあるため、本章の例だけで全契約の計算が検証済みとは判断しません。

## 利用者・権限

管理者、システム担当者、総務、営業、パートナー、経営者が役割と担当範囲に応じて利用します。

## 前提・入力・処理・出力

有効な個別案件と金額データを前提に、日付、勤務時刻、距離、経費、区分を入力します。休日、深夜、深夜超過、不足、距離超過等を計算し、月次承認と精算へ渡します。

## 関連機能・前後工程

日報提出で受領を確認し、個別案件・料金・計算ルールを参照します。承認済み日報から請求・支払下書きを作ります。

## 主要API・データ

日報本体、監査ログ、確認スナップショット、月次承認、距離月次結果を分けて保持します。Excel取込はバッチ・ファイル・行・監査を別管理します。

対象月は案件の締め期間を表します。20日締めの11月分は10月21日〜11月20日です。日報一覧APIの `period` は開始日・終了日・適用締日・入力日一覧を返し、承認スナップショットと精算下書きにも保存します。Excel/CSV取込は同じ勤務日判定を使います。

`GET /api/daily-reports/period-migration?project_id=...` は既存暦月の移行差分と署名を返します。事務・管理者が画面で前後期間と移動する日報を確認し、理由と署名を `POST /api/daily-reports/period-migration` へ渡すと未確定分だけを一括移行します。金額は再計算しません。別担当者による変更があれば再プレビューを求めます。

## 状態・制御・注意点

未入力、入力中、申請可能、承認待ち、承認済み、差戻し、訂正中を区別します。承認後の直接上書きを避け、差異が出た場合は訂正中として扱います。

既存期間は `legacy_calendar` として保持し、日次確認・月次承認・精算で保護された期間は移行しません。締日変更は保存済み期間を変更しません。前期間から引き継ぐ境界調整は「移行期間」と表示し、保存済み後続期間と矛盾する登録は拒否します。

## 現行業務との乖離・確認事項

全入力項目、元Excel数式、単位、丸め、休日・例外運用の最終照合がD-01として残っています。

## 今後の改善

承認専用ビュー、入力根拠の説明、数式期待値ケース拡充、差分訂正依頼を検討します。
