(() => {
  const ui = {
    async open(ctx){this.ctx=ctx;this.kit=window.LinksFeatureKit.createFeatureKit(ctx);this.ym=this.kit.currentYearMonth();await this.list();}, money:v=>Number(v||0).toLocaleString('ja-JP'), status:v=>({draft:'下書き',sales_reviewed:'営業確認済み',finalized:'最終確定',cancelled:'取消済み'}[v]||v||'下書き'),
    async list(message=''){this.ctx.renderLoading();const [targets,rows]=await Promise.all([this.ctx.api(`/api/payments/targets?target_year_month=${this.ym}`),this.ctx.api(`/api/payments?target_year_month=${this.ym}`)]);if(!targets.res.ok)return this.error(targets.data?.message);this.targets=targets.data.targets||[];const todo=this.targets.map((x,i)=>`<tr><td>${this.ctx.escapeHtml(x.partner_name)}</td><td>${x.report_count}</td><td>${this.money(x.gross_amount)}円</td><td><button class="btn btn-small" data-draft="${i}">下書き作成</button></td></tr>`).join('');const issued=(rows.data?.payments||[]).map(x=>`<tr><td>#${x.payment_id}</td><td>${this.ctx.escapeHtml(x.partner_name||x.partner_id)}</td><td>${this.money(x.final_transfer_amount)}円</td><td>${this.status(x.settlement_status)}</td><td><button class="btn btn-ghost btn-small" data-open="${x.payment_id}">開く</button></td></tr>`).join('');this.ctx.app.innerHTML=this.kit.shell('支払',`<section class="panel">${message?`<p class="flash">${this.ctx.escapeHtml(message)}</p>`:''}<div class="toolbar"><input id="ym" type="month" value="${this.ym}"><button id="load" class="btn">表示</button></div><h3 class="section-title">月次承認済み日報から作成</h3><div class="table-wrap"><table class="data-table data-table-compact"><thead><tr><th>パートナー</th><th>日報</th><th>支払総額</th><th>操作</th></tr></thead><tbody>${todo||'<tr><td colspan="4">対象なし</td></tr>'}</tbody></table></div><h3 class="section-title">支払一覧</h3><div class="table-wrap"><table class="data-table data-table-compact"><thead><tr><th>No</th><th>パートナー</th><th>振込予定額</th><th>状態</th><th>操作</th></tr></thead><tbody>${issued||'<tr><td colspan="5">なし</td></tr>'}</tbody></table></div></section>`);this.kit.bindShell();document.getElementById('load').onclick=()=>{this.ym=document.getElementById('ym').value;this.list();};document.querySelectorAll('[data-draft]').forEach(b=>b.onclick=()=>this.draft(this.targets[Number(b.dataset.draft)]));document.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>{this.kit.pushNav(()=>this.list());this.detail(Number(b.dataset.open));});},
    async draft(t){const amount=window.prompt('手動調整額（不要なら0）','0');if(amount===null)return;const reason=Number(amount)<0?window.prompt('負額調整の理由',''):'';if(Number(amount)<0&&!reason)return window.alert('負額調整には理由が必要です');const salary=window.confirm('給与明細書も発行しますか？');const r=await this.ctx.api('/api/settlements/payment/drafts',{method:'POST',body:JSON.stringify({target_year_month:this.ym,partner_id:t.partner_id,daily_report_ids:t.report_ids,closing_date:t.closing_date||'end',issue_salary_statement:salary,adjustments:Number(amount)?[{item_name:'手動調整',amount:Number(amount),reason}]:[]})});if(!r.res.ok)return window.alert(r.data?.message||'作成失敗');this.kit.pushNav(()=>this.list());this.detail(r.data.settlement_id);},
    async detail(id){this.ctx.renderLoading();const r=await this.ctx.api(`/api/settlements/payment/${id}`);if(!r.res.ok)return this.error(r.data?.message,()=>this.list());const {settlement:s,workflow:w,lines=[],documents=[]}=r.data;const lineRows=lines.map(x=>`<tr><td>${this.ctx.escapeHtml(x.item_name)}</td><td>${this.ctx.escapeHtml(x.line_type)}</td><td>${this.money(x.amount)}円</td><td>${this.ctx.escapeHtml(x.tax_category)}</td></tr>`).join('');const docs=documents.map(x=>`<li>${this.ctx.escapeHtml(x.document_number)} <a href="/api/settlements/documents/${x.settlement_document_id}/download">PDF</a></li>`).join('');this.ctx.app.innerHTML=this.kit.shell(`支払 #${id}`,`<section class="panel"><div class="preview-sheet"><h3>支払確定内容</h3><p>対象月: ${s.target_year_month} / 状態: <strong>${this.status(w?.status)}</strong></p><p>支払総額 ${this.money(s.gross_amount)}円 / 最終振込額 <strong>${this.money(s.final_transfer_amount)}円</strong></p></div><div class="table-wrap"><table class="data-table data-table-compact"><thead><tr><th>項目</th><th>種別</th><th>金額</th><th>税区分</th></tr></thead><tbody>${lineRows}</tbody></table></div><h3 class="section-title">PDF帳票</h3><ul>${docs||'<li>最終確定後に発行します</li>'}</ul><div class="btn-row">${w?.status==='draft'?'<button id="review" class="btn">営業確認</button>':''}${w?.status==='sales_reviewed'?'<select id="cycle"></select><button id="finalize" class="btn">最終確定・PDF発行</button>':''}${w?.status==='finalized'?'<button id="cancel" class="btn btn-ghost">取消</button>':''}<button id="back" class="btn btn-ghost">一覧へ</button></div></section>`,{onBack:()=>this.list()});this.kit.bindShell({onBack:()=>this.list()});document.getElementById('back').onclick=()=>this.list();document.getElementById('review')?.addEventListener('click',()=>this.action(`/api/settlements/payment/${id}/sales-review`,{},()=>this.detail(id)));document.getElementById('cancel')?.addEventListener('click',()=>{const reason=window.prompt('取消理由','');if(reason)this.action(`/api/settlements/payment/${id}/cancel`,{reason},()=>this.detail(id));});if(w?.status==='sales_reviewed')this.cycles(id);},
    async cycles(id){const r=await this.ctx.api(`/api/cash-management/cycles?target_year_month=${this.ym}`);const e=document.getElementById('cycle');if(!e)return;e.innerHTML=(r.data?.cycles||[]).map(x=>`<option value="${x.cash_cycle_id}">${x.cycle_code==='end'?'末日':Number(x.cycle_code)}日回</option>`).join('');document.getElementById('finalize').onclick=()=>this.action(`/api/settlements/payment/${id}/finalize`,{cash_cycle_id:Number(e.value)},()=>this.detail(id));},
    async action(path,body,next){const r=await this.ctx.api(path,{method:'POST',body:JSON.stringify(body)});if(!r.res.ok)return window.alert(r.data?.message||'処理失敗');next();},error(message,onBack){this.ctx.app.innerHTML=this.kit.shell('支払',`<section class="panel"><p class="error">${this.ctx.escapeHtml(message||'取得失敗')}</p></section>`,{onBack});this.kit.bindShell({onBack});},
  };window.LinksPayments=ui;
})();
